<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/global.css">
</head>
<body>
    <input type="text" value="<%= locals.user ? locals.user.name : null %>">
    <input type="text" value="<%= locals.user ? locals.user.name : null %>">
    <input type="text" value="<%= locals.user ? locals.user.name : null %>">
</body>
</html>